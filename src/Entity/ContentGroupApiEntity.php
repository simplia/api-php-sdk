<?php

/**
 * !!! AUTOGENERATED FROM THE /api/3 OPENAPI DOCUMENT BY api3:sdk:generate
 * !!! DO NOT EDIT — change the generator (src/Api/Sdk in the shop repository) instead
 */

declare(strict_types=1);

namespace Simplia\Api\Entity;

use Simplia\Api\FieldConfig\ContentGroupApiFieldConfig;

/**
 * A content group: a set of content — a category tree, and text pages, banners and articles kept apart the same way — that a storefront can be pointed at, one group per kind of content. The record `Category.content_group` points at, and the value the `content_group` filter of `GET /categories` takes. Every group is listed.
 */
final class ContentGroupApiEntity extends AbstractApiEntity {
    /**
     * Numeric identifier of the content group; the value `Category.content_group.id` carries and the `content_group` filter of `GET /categories` takes.
     */
    public function getId(): int {
        return $this->readInt('id');
    }

    /**
     * Name of the content group, such as `default` or `b2b`.
     */
    public function getName(): string {
        return $this->readString('name');
    }

    public static function createFieldConfig(): ContentGroupApiFieldConfig {
        return new ContentGroupApiFieldConfig();
    }
}
