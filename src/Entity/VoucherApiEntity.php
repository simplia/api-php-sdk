<?php

/**
 * !!! AUTOGENERATED FROM THE /api/3 OPENAPI DOCUMENT BY api3:sdk:generate
 * !!! DO NOT EDIT — change the generator (src/Api/Sdk in the shop repository) instead
 */

declare(strict_types=1);

namespace Simplia\Api\Entity;

use Simplia\Api\Entity\VoucherGroup\VoucherGroupReferenceApiEntity;
use Simplia\Api\FieldConfig\VoucherApiFieldConfig;
use Simplia\Api\Money;

/**
 * A discount code a customer redeems at checkout: one code belonging to a voucher group (the campaign that defines what the discount does), with its validity window, use limit and usage count. Codes are created from a group, then locked, activated and applied through the action operations. The list holds the codes of the content group (storefront tree) being called, or the one `content_group` names; any code can be read by id or by its code.
 */
final class VoucherApiEntity extends AbstractApiEntity {
    /**
     * Numeric identifier of the voucher; the `{id}` of `GET /vouchers/{id}`.
     */
    public function getId(): int {
        return $this->readInt('id');
    }

    /**
     * The code the customer types; the `{code}` of the action operations.
     */
    public function getCode(): string {
        return $this->readString('code');
    }

    /**
     * When the code becomes redeemable, compared by calendar day. Null when there is no start bound.
     */
    public function getStartsAt(): ?\DateTimeImmutable {
        return $this->readDateTimeOrNull('starts_at');
    }

    /**
     * Last day the code is redeemable, inclusive, compared by calendar day; the time part is always midnight. Null when the code never expires.
     */
    public function getEndsAt(): ?\DateTimeImmutable {
        return $this->readDateTimeOrNull('ends_at');
    }

    /**
     * Face value of an amount-based code, in the shop's main currency. Null for a percentage or other non-amount code.
     */
    public function getValue(): ?Money {
        return $this->readMoneyOrNull('value');
    }

    /**
     * Whether the code is in circulation. A code created here is active at once; a prepared (inactive) code becomes active through `POST /vouchers/{code}/activate`.
     */
    public function isActive(): bool {
        return $this->readBool('active');
    }

    /**
     * The voucher group the code belongs to. Null for a code without a group, which is never valid.
     */
    public function getGroup(): ?VoucherGroupReferenceApiEntity {
        return $this->readEntityOrNull('group', VoucherGroupReferenceApiEntity::class);
    }

    /**
     * How many times the code may be redeemed, from its group. 0 or null means unlimited.
     */
    public function getMaxUses(): ?int {
        return $this->readIntOrNull('max_uses');
    }

    /**
     * How many times the code has been redeemed.
     */
    public function getTotalUses(): int {
        return $this->readInt('total_uses');
    }

    /**
     * Whether the code can be redeemed right now: active, in a group, not used up, and within its validity window.
     */
    public function isValid(): bool {
        return $this->readBool('valid');
    }

    public static function createFieldConfig(): VoucherApiFieldConfig {
        return new VoucherApiFieldConfig();
    }
}
