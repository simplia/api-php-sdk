<?php

/**
 * !!! AUTOGENERATED FROM THE /api/3 OPENAPI DOCUMENT BY api3:sdk:generate
 * !!! DO NOT EDIT — change the generator (src/Api/Sdk in the shop repository) instead
 */

declare(strict_types=1);

namespace Simplia\Api\Entity;

use Simplia\Api\Enum\LabelAppliesTo;
use Simplia\Api\FieldConfig\LabelApiFieldConfig;

/**
 * A label the shop pins on records — a badge such as "new" or "sale" on a product, a mark on a variant or on a storage location: its code, its name in the request language, its colour and picture, the kinds of record it applies to, who sees it and when. `GET /labels` lists every label; a product's `labels` list points at them by `id`.
 */
final class LabelApiEntity extends AbstractApiEntity {
    /**
     * Label id, the `id` an entry of a product's `labels` carries.
     */
    public function getId(): int {
        return $this->readInt('id');
    }

    /**
     * A short code the shop gave the label: lowercase letters, digits and underscores, unique among the shop's labels. Null when the label has none.
     */
    public function getCode(): ?string {
        return $this->readStringOrNull('code');
    }

    /**
     * Name in the request language. Null when the label has no name in that language.
     */
    public function getName(): ?string {
        return $this->readStringOrNull('name');
    }

    /**
     * The label's colour as a hexadecimal `#rrggbb` value. Null when the shop set none.
     */
    public function getColor(): ?string {
        return $this->readStringOrNull('color');
    }

    /**
     * The label's picture, shown in place of its name when the shop stored one. Null when none is stored.
     */
    public function getImage(): ?ImageApiEntity {
        return $this->readEntityOrNull('image', ImageApiEntity::class);
    }

    /**
     * The kinds of record the label can be pinned on: `product`, `variant`, `storage_location`. Empty when the shop has not picked any.
     * @return list<LabelAppliesTo>
     */
    public function getAppliesTo(): array {
        return $this->readEnumList('applies_to', LabelAppliesTo::class);
    }

    /**
     * Whether retail customers see the label.
     */
    public function isPublicRetail(): bool {
        return $this->readBool('public_retail');
    }

    /**
     * Whether wholesale customers see the label.
     */
    public function isPublicWholesale(): bool {
        return $this->readBool('public_wholesale');
    }

    /**
     * Position among the shop's labels; lower first. The order of `sort=priority` and of a product's `labels` list.
     */
    public function getPriority(): int {
        return $this->readInt('priority');
    }

    /**
     * The start of the label's validity window; before it the label is not shown.
     */
    public function getStartsAt(): \DateTimeImmutable {
        return $this->readDateTime('starts_at');
    }

    /**
     * The end of the label's validity window; after it the label is not shown.
     */
    public function getEndsAt(): \DateTimeImmutable {
        return $this->readDateTime('ends_at');
    }

    public static function createFieldConfig(): LabelApiFieldConfig {
        return new LabelApiFieldConfig();
    }
}
