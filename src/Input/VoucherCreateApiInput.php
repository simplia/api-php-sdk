<?php

/**
 * !!! AUTOGENERATED FROM THE /api/3 OPENAPI DOCUMENT BY api3:sdk:generate
 * !!! DO NOT EDIT — change the generator (src/Api/Sdk in the shop repository) instead
 */

declare(strict_types=1);

namespace Simplia\Api\Input;

use Simplia\Api\Money;

/**
 * Creates one new, already active discount code in a voucher group. The code itself is generated by the shop.
 */
final class VoucherCreateApiInput extends AbstractApiInput {
    /**
     * Identifier of the voucher group the code belongs to (`Voucher.group.id`). Required; 404 when it does not exist.
     */
    public function setGroupId(?int $groupId): self {
        $this->params['group_id'] = $groupId;

        return $this;
    }

    /**
     * Face value of this code, in the shop's main currency, overriding the group's amount. Allowed only for an amount-based group, otherwise a 422 violation. Omit to inherit the group's amount.
     */
    public function setValue(?Money $value): self {
        $this->params['value'] = $value;

        return $this;
    }

    /**
     * When the code becomes redeemable. Omit for now.
     */
    public function setStartsAt(?\DateTimeInterface $startsAt): self {
        $this->params['starts_at'] = $startsAt?->format(\DateTimeInterface::ATOM);

        return $this;
    }

    /**
     * Last day the code is redeemable; must not be earlier than `starts_at`. Omit to derive it from the group: its validity span counted from now, else its own end date, else no expiry.
     */
    public function setEndsAt(?\DateTimeInterface $endsAt): self {
        $this->params['ends_at'] = $endsAt?->format(\DateTimeInterface::ATOM);

        return $this;
    }

    /**
     * Identifier of the registered customer the code is bound to (`User.id`); 404 when it does not exist. Not returned by any read.
     */
    public function setUserId(?int $userId): self {
        $this->params['user_id'] = $userId;

        return $this;
    }
}
