<?php

/**
 * !!! AUTOGENERATED FROM THE /api/3 OPENAPI DOCUMENT BY api3:sdk:generate
 * !!! DO NOT EDIT — change the generator (src/Api/Sdk in the shop repository) instead
 */

declare(strict_types=1);

namespace Simplia\Api3\Endpoint;

use Simplia\Api3\Entity\PackageApiEntity;
use Simplia\Api3\FieldConfig\PackageApiFieldConfig;
use Simplia\Api3\Request\PackagesApiRequest;
use Simplia\Api3\RequestHandler;

/**
 * The operations of /api/3/packages.
 */
final class PackagesApiEndpoint extends AbstractApiEndpoint {
    /**
     * List shipments.
     * Permission: api:packages:list.
     * Every row, page by page (the Link header is followed for you).
     * @return \Generator<int, PackageApiEntity>
     */
    public function iterate(PackagesApiRequest $request, PackageApiFieldConfig $fields, int $perPage = 100): \Generator {
        foreach ($this->iterateList('packages', [], $request, $fields, $perPage) as $row) {
            yield new PackageApiEntity($row);
        }
    }

    /**
     * How many rows iterate() would answer, without fetching them.
     */
    public function count(PackagesApiRequest $request): int {
        return $this->countList('packages', [], $request);
    }

    /**
     * Get a shipment.
     * Permission: api:package:get.
     */
    public function get(int $id, PackageApiFieldConfig $fields): ?PackageApiEntity {
        $result = $this->singleResult('packages/' . $id, [], $fields);

        return $result === null ? null : new PackageApiEntity($result);
    }
}
