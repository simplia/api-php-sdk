<?php

/**
 * !!! AUTOGENERATED FROM OPENAPI SPECIFICATION
 * !!! DON'T EDIT AND DON'T SEND PULL REQUESTS DIRECTLY FOR THIS FILE
 */

declare(strict_types=1);

namespace Simplia\Api\Endpoint;

use Generator;
use Simplia\Api\Entity\PackageApiEntity;
use Simplia\Api\FieldConfig\PackageApiFieldConfig;
use Simplia\Api\Request\PackageApiRequest;

class PackagesApiEndpoint extends AbstractApiEndpoint {
    /**
     * List packages
     * @param PackageApiRequest $request
     * @param PackageApiFieldConfig $fields
     * @param int $batchSize
     * @return Generator|PackageApiEntity[]
     */
    final public function iterate(
        PackageApiRequest $request,
        PackageApiFieldConfig $fields,
        int $batchSize = 100
    ): Generator {
        foreach ($this->iterateList('packages', [], $request, $fields, $batchSize) as $row) {
            yield new PackageApiEntity($row);
        }
        yield from [];
    }

    /**
     * Get single package
     */
    final public function get(int $id, PackageApiFieldConfig $fields): ?PackageApiEntity {
        $result = $this->singleResult('packages/' . $id, [], $fields);

        return $result ? new PackageApiEntity($result) : null;
    }
}
