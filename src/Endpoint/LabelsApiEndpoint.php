<?php

/**
 * !!! AUTOGENERATED FROM THE /api/3 OPENAPI DOCUMENT BY api3:sdk:generate
 * !!! DO NOT EDIT — change the generator (src/Api/Sdk in the shop repository) instead
 */

declare(strict_types=1);

namespace Simplia\Api\Endpoint;

use Simplia\Api\Entity\LabelApiEntity;
use Simplia\Api\FieldConfig\LabelApiFieldConfig;
use Simplia\Api\Request\LabelsApiRequest;
use Simplia\Api\RequestHandler;

/**
 * The operations of /api/3/labels.
 */
final class LabelsApiEndpoint extends AbstractApiEndpoint {
    /**
     * List labels.
     * Permission: api:labels:list.
     * Every row, page by page (the Link header is followed for you).
     * @return \Generator<int, LabelApiEntity>
     */
    public function iterate(LabelsApiRequest $request, LabelApiFieldConfig $fields, int $perPage = 100): \Generator {
        foreach ($this->iterateList('labels', [], $request, $fields, $perPage) as $row) {
            yield new LabelApiEntity($row);
        }
    }

    /**
     * How many rows iterate() would answer, without fetching them.
     */
    public function count(LabelsApiRequest $request): int {
        return $this->countList('labels', [], $request);
    }

    /**
     * Get a label.
     * Permission: api:labels:get.
     */
    public function get(int $id, LabelApiFieldConfig $fields): ?LabelApiEntity {
        $result = $this->singleResult('labels/' . $id, [], $fields);

        return $result === null ? null : new LabelApiEntity($result);
    }
}
