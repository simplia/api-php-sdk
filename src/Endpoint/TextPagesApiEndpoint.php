<?php

/**
 * !!! AUTOGENERATED FROM THE /api/3 OPENAPI DOCUMENT BY api3:sdk:generate
 * !!! DO NOT EDIT — change the generator (src/Api/Sdk in the shop repository) instead
 */

declare(strict_types=1);

namespace Simplia\Api3\Endpoint;

use Simplia\Api3\Entity\TextPageApiEntity;
use Simplia\Api3\FieldConfig\TextPageApiFieldConfig;
use Simplia\Api3\Input\TextPageApiInput;
use Simplia\Api3\Request\TextPagesApiRequest;
use Simplia\Api3\RequestHandler;

/**
 * The operations of /api/3/text-pages.
 */
final class TextPagesApiEndpoint extends AbstractApiEndpoint {
    /**
     * List text pages.
     * Permission: api:text-pages:list.
     * Every row, page by page (the Link header is followed for you).
     * @return \Generator<int, TextPageApiEntity>
     */
    public function iterate(TextPagesApiRequest $request, TextPageApiFieldConfig $fields, int $perPage = 100): \Generator {
        foreach ($this->iterateList('text-pages', [], $request, $fields, $perPage) as $row) {
            yield new TextPageApiEntity($row);
        }
    }

    /**
     * How many rows iterate() would answer, without fetching them.
     */
    public function count(TextPagesApiRequest $request): int {
        return $this->countList('text-pages', [], $request);
    }

    /**
     * Create a text page.
     * Permission: api:text-pages:add.
     * `$idempotencyKey` is sent as Idempotency-Key; a fresh UUID when null.
     */
    public function create(TextPageApiInput $input, ?TextPageApiFieldConfig $fields = null, ?string $idempotencyKey = null): TextPageApiEntity {
        $result = $this->request('POST', 'text-pages', [], $input, $fields, RequestHandler::JSON, $idempotencyKey);

        return new TextPageApiEntity($result ?? throw new \UnexpectedValueException('The API answered without a body.'));
    }

    /**
     * Get a text page.
     * Permission: api:text-pages:get.
     */
    public function get(int $id, TextPageApiFieldConfig $fields): ?TextPageApiEntity {
        $result = $this->singleResult('text-pages/' . $id, [], $fields);

        return $result === null ? null : new TextPageApiEntity($result);
    }

    /**
     * Update a text page.
     * Permission: api:text-pages:update.
     */
    public function update(int $id, TextPageApiInput $input, ?TextPageApiFieldConfig $fields = null): TextPageApiEntity {
        $result = $this->request('PATCH', 'text-pages/' . $id, [], $input, $fields, RequestHandler::MERGE_PATCH);

        return new TextPageApiEntity($result ?? throw new \UnexpectedValueException('The API answered without a body.'));
    }

    /**
     * Delete a text page.
     * Permission: api:text-pages:delete.
     */
    public function delete(int $id): void {
        $this->request('DELETE', 'text-pages/' . $id, [], null, null);
    }
}
