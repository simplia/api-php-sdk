<?php

/**
 * !!! AUTOGENERATED FROM THE /api/3 OPENAPI DOCUMENT BY api3:sdk:generate
 * !!! DO NOT EDIT — change the generator (src/Api/Sdk in the shop repository) instead
 */

declare(strict_types=1);

namespace Simplia\Api3\Request;

/**
 * The filters and the sort of `stock-items.list` (GET /api/3/stock-items).
 */
final class StockItemsApiRequest extends AbstractApiRequest {
    /**
     * When true, only items with a positive total quantity in stock. false is a no-op.
     */
    public function whereInStock(bool $inStock): self {
        $this->params['in_stock'] = $inStock ? 'true' : 'false';

        return $this;
    }

    /**
     * Filter by item code (the code property). Repeatable (`codes[]=A&codes[]=B`); every element must be a non-blank string of at most 255 characters (422 otherwise); unknown codes are silently absent.
     * @param list<string> $codes
     */
    public function whereCodes(array $codes): self {
        $this->params['codes'] = $codes;

        return $this;
    }

    /**
     * Filter by supplier code (the code_supplier property). Repeatable (`codes_supplier[]=A&codes_supplier[]=B`); every element must be a non-blank string of at most 255 characters (422 otherwise); unknown codes are silently absent.
     * @param list<string> $codesSupplier
     */
    public function whereCodesSupplier(array $codesSupplier): self {
        $this->params['codes_supplier'] = $codesSupplier;

        return $this;
    }

    /**
     * Only items whose product is supplied by this supplier, or whose product has no supplier of its own and whose brand is.
     */
    public function whereSupplierId(int $supplierId): self {
        $this->params['supplier_id'] = $supplierId;

        return $this;
    }

    /**
     * Filter by stock item ids. Repeatable (`ids[]=1&ids[]=2`); every element must be an integer (digits only; 422 otherwise); unknown ids are silently absent.
     * @param list<int> $ids
     */
    public function whereIds(array $ids): self {
        $this->params['ids'] = $ids;

        return $this;
    }

    public function orderByIdAsc(): self {
        $this->params['sort'] = 'id';

        return $this;
    }

    public function orderByIdDesc(): self {
        $this->params['sort'] = '-id';

        return $this;
    }
}
