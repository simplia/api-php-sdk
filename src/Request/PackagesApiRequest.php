<?php

/**
 * !!! AUTOGENERATED FROM THE /api/3 OPENAPI DOCUMENT BY api3:sdk:generate
 * !!! DO NOT EDIT — change the generator (src/Api/Sdk in the shop repository) instead
 */

declare(strict_types=1);

namespace Simplia\Api3\Request;

/**
 * The filters and the sort of `packages.list` (GET /api/3/packages).
 */
final class PackagesApiRequest extends AbstractApiRequest {
    /**
     * Code of the carrier the shipment was handed to. Shipments not yet on a carrier hand-over sheet are not returned.
     */
    public function whereCarrier(string $carrier): self {
        $this->params['carrier'] = $carrier;

        return $this;
    }

    /**
     * Tracking number of any parcel of the shipment.
     */
    public function whereCode(string $code): self {
        $this->params['code'] = $code;

        return $this;
    }

    /**
     * Identifier of the carrier hand-over sheet the shipment was dispatched on. Not the shipment's own id, which is ids.
     */
    public function whereShipmentId(int $shipmentId): self {
        $this->params['shipment_id'] = $shipmentId;

        return $this;
    }

    /**
     * Owning order code (the human code; use order_id for the numeric id).
     */
    public function whereOrderCode(string $orderCode): self {
        $this->params['order_code'] = $orderCode;

        return $this;
    }

    /**
     * Owning order id (the numeric id); use order_code for the human code.
     */
    public function whereOrderId(int $orderId): self {
        $this->params['order_id'] = $orderId;

        return $this;
    }

    /**
     * Shipments handed to the carrier on/after this day (the date of the hand-over sheet, not created_at). Shipments not yet on a sheet are not returned. A date-time (2026-09-09T14:30:00, optionally with an offset) bounds by that instant instead.
     */
    public function whereShipmentDateFrom(\DateTimeInterface|string $shipmentDateFrom): self {
        $this->params['shipment_date_from'] = $shipmentDateFrom instanceof \DateTimeInterface ? $shipmentDateFrom->format(\DateTimeInterface::ATOM) : $shipmentDateFrom;

        return $this;
    }

    /**
     * Shipments handed to the carrier on/before this day (the date of the hand-over sheet, not created_at), the whole day included. Shipments not yet on a sheet are not returned. A date-time (2026-09-09T14:30:00, optionally with an offset) bounds by that instant instead.
     */
    public function whereShipmentDateTo(\DateTimeInterface|string $shipmentDateTo): self {
        $this->params['shipment_date_to'] = $shipmentDateTo instanceof \DateTimeInterface ? $shipmentDateTo->format(\DateTimeInterface::ATOM) : $shipmentDateTo;

        return $this;
    }

    /**
     * Filter by package ids. Repeatable (`ids[]=1&ids[]=2`); every element must be an integer (digits only; 422 otherwise); unknown ids are silently absent.
     * @param list<int> $ids
     */
    public function whereIds(array $ids): self {
        $this->params['ids'] = $ids;

        return $this;
    }

    public function orderByCreatedAtAsc(): self {
        $this->params['sort'] = 'created_at';

        return $this;
    }

    public function orderByCreatedAtDesc(): self {
        $this->params['sort'] = '-created_at';

        return $this;
    }
}
