{
    "name": "simplia/api",
    "description": "PHP client for the Simplia /api/3, generated from the OpenAPI document by the shop's own generator (api3:sdk:generate). The 3.x line of simplia/api; the /api/2 client is the 0.1.x line.",
    "type": "library",
    "license": "proprietary",
    "require": {
        "php": ">=8.3",
        "ext-json": "*",
        "nyholm/psr7": "^1.8",
        "psr/http-client": "^1.0",
        "psr/http-message": "^1.1 || ^2.0"
    },
    "require-dev": {
        "phpstan/phpstan": "^2.0"
    },
    "suggest": {
        "phpstan/extension-installer": "Loads the package's PHPStan rule without an includes: line"
    },
    "autoload": {
        "psr-4": {
            "Simplia\\Api\\": "src/"
        }
    },
    "config": {
        "sort-packages": true
    },
    "extra": {
        "phpstan": {
            "includes": ["phpstan-extension.neon"]
        }
    }
}
